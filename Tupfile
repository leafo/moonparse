.gitignore

: parse2.moon |> moon %f > %o |> parse2.peg

: foreach *.peg |> peg -o%o %f |> %B.h
: foreach *.cpp | parse.h parse2.h |> g++ -I/usr/include/lua5.1 -Wall -fPIC -c %f -o %o |> %B.o
: *.o |> g++ -shared -o %o -L/usr/lib %f |> moonparse.so


: foreach *.moon |> moonc %f |> %B.lua
